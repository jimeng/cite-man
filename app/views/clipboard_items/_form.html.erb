<%= form_for(@clipboard_item) do |f| %>
  <% if @clipboard_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clipboard_item.errors.count, "error") %> prohibited this clipboard_item from being saved:</h2>

      <ul>
      <% @clipboard_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :citation %><br />
    <%= f.text_area :citation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
